/**
 * Implement Gatsby's Node APIs in this file.
 *
 * See: https://www.gatsbyjs.com/docs/node-apis/
 */

// You can delete this file if you're not using it

/*exports.createPages = async ({ actions }) => {
    const { createPage } = actions;
    
    const openGraphImage = createOpenGraphImage(createPage, {
        path: "/og-image/index.png",
        component: path.resolve(`src/templates/index.og-image.js`),
        size: {
            width: 50,
            height: 50,
        },
        context: {
            description: "I am Tibor Borsos, the creator and owner of BioBey, a consultancy specialising in training and supporting analytical labs, especially in the field of LC-MC bioanalysis",
        },
    });
};  */